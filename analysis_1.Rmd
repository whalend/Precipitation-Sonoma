---
title: "Sonoma precipitation data analysis1"
author: "whalen"
date: "November 18, 2014"
output: html_document
---

Outliers have been replaced with `NA` (see `explore_report.Rmd` for details). Here I load the datasets with the `NA`s inserted and start setting things up for analyses - thin plate spline and/or regression kriging.

Load libraries I plan to use for data manipulation (`dplyr`) and plotting (`ggplot2`).
```{r load libraries, echo=TRUE, message=FALSE}
library(ggplot2); library(plyr); library(raster); library(dplyr)
```

Read-in the data sets, one daily and one hourly, and then plot. For outlier replacement in these data sets I settled on a daily precipitation threshold of greater than or equal to 6.5 inches, and an hourly precipitation threshold of greater than or equal to 2 inches for the data from our rain gagues.

```{r read in daily data, echo=TRUE}
dy_rg_data <- readRDS("dy_rg_data_corrected.rds")
summary(dy_rg_data)
str(dy_rg_data)
filter(dy_rg_data, is.na(daily_ppt)) %>% 
      select(date, stationid) %>% 
      arrange(date)
na.2004 <- (filter(dy_rg_data, is.na(daily_ppt), year == 2004))
na.2004
rm(na.2004)

(filter(dy_rg_data, is.na(daily_ppt), year == 2004, month == 12))

dy_rg_data_tmp <- dy_rg_data
dy_rg_data_tmp[is.na(dy_rg_data_tmp)] <- -2

ggplot(data = dy_rg_data_tmp, aes(x = date, y = daily_ppt)) +
      geom_point() +
      facet_grid(stationid ~ .)
```

Make data frame of monthly totals and plot across all years

```{r summarise daily to monthly totals, echo=TRUE, eval=FALSE}
# Monthly precipitation (ppt) by year, month, and gauge id
monthly <- dy_rg_data %>% 
      group_by(year, month, stationid) %>%# This grouping order causes reordering by year, month, then id
      summarise(monthly_ppt = sum(daily_ppt, na.rm = T), 
                monthly_ppt_mm = sum(daily_ppt, na.rm = T) * 25.4)
monthly
# Add date factor variable
monthly <- mutate(monthly, date = paste(year,"-",month, sep = ""))
# Convert date factor to `date` format for plotting
monthly$date <- as.Date(paste(monthly$date, "-01", sep=""))
monthly

# Aggregate to mean monthly precip across all gauges, so exclude id from the data frame `monthly_ppt_id`
monthly_ppt <- monthly %>%
      group_by(year, month, date) %>%
      summarise(avg_monthly_ppt = mean(monthly_ppt, na.rm=TRUE))
summary(monthly_ppt)
filter(monthly_ppt, is.na(avg_monthly_ppt))
```

Plot total monthly rainfall data

```{r plotting monthly data, echo=TRUE, eval=FALSE, fig.height=9, fig.width=6}
# Plot monthly rainfall grouped by plot ####
# monthly_ppt <- ungroup(monthly_ppt)
# monthly$stationid <- as.factor(monthly$stationid)
mp_id <- ggplot(data = monthly, #%>%
            # filter(id == "cook" | id == "kunde" | id == "sawi"),
            # group_by(id),
       aes(x = date, y = monthly_ppt_mm)) + 
      # geom_point(size=2) +
      ylab("rainfall (mm)")

library(scales)
mp_id + 
      # geom_point(size=2) +
      geom_bar(stat = "identity") +
      facet_grid(stationid ~ .) +
      theme_bw() + 
      theme(legend.position = "none") +
      scale_x_date(breaks = date_breaks("6 months"), 
                   labels = date_format("%m/%y"),
                   date_minor_breaks = "1 month") +
      xlab("date (month/year)") + 
      ggtitle("Total Monthly Rainfall by Station 2004-2013")

ggsave("figures/mp_id.png", width = 7, height = 5, scale = 1.5)
# mp + geom_boxplot()
# Adjust the way the date is scaled on the x-axis
#mp + scale_x_date(date)

# Plot monthly data averaged across plots
mp <- ggplot(data = monthly_ppt,
            #filter(id == "cook" | id == "kunde" | id == "sawi"),
            #group_by(id),
       aes(x = date, y = avg_monthly_ppt)) + 
      geom_point(size=2) + geom_line() +
      ylab("rainfall (inches)")

library(scales)
mp + geom_line() +
      theme_bw() + theme(legend.position = "none") +
      scale_x_date(date_breaks = "1 year", 
                   labels = date_format("%m/%y"),
                   date_minor_breaks = "1 month") +
      xlab("date (month/year)") + ggtitle("Average Monthly Rainfall Across All Plots 2004-2013")

mp0809 <- ggplot(data = monthly_ppt %>% 
                       filter(date >= "2008-07-01", date <= "2009-06-30"),
            #filter(id == "cook" | id == "kunde" | id == "sawi"),
            #group_by(id),
       aes(x = date, y = avg_monthly_ppt)) + 
      geom_bar(stat = "identity") +
      # geom_point(size=2) + geom_line() +
      ylab("rainfall (inches)") +
      theme_bw() + theme(legend.position = "none") +
      scale_x_date(date_breaks = "1 month", 
                   labels = date_format("%m/%y"),
                   date_minor_breaks = "1 month")

ggsave("figures/avg_mp.png", width = 7, height = 5, scale = 1.5)
```

```{r export plot to file, echo=TRUE, eval=TRUE}
# `ggsave` exports the current `ggplot` object
ggsave("figures/avg_mp.png", width = 7, height = 5, scale = 1.5)
```


I also have data at an hourly resolution, but am not going to work with that for now
```{r hourly data, eval=FALSE, echo=TRUE}
hr_rg_data <- readRDS("hr_rg_data.rds")
summary(hr_rg_data)
qplot(id, hourly_ppt, data=hr_rg_data)
#Replace hourly_ppt values >= 1 inch
hr_rg_data$hourly_ppt[hr_rg_data$hourly_ppt>=1] <- NA

qplot(month, monthly_ppt, data = hr_rg_data %>%
      filter(year==2006) %>%
            group_by(id, year, month) %>%
            summarize(monthly_ppt=sum(hourly_ppt)), color = id,
      main="Monthly total from hourly data - 2006")
```

I want to group the daily data into data frames by month and year so that I can create an interpolation for precipitation for each unique month-year combination from each location. For example:

```{r group by year month, echo=TRUE, eval=F}
ppt2004_01 <- dy_rg_data %>% #
      group_by(id, year, month) %>% 
      #filter(year==2004, month==1) %>%
      summarise(monthly_ppt = sum(daily_ppt))
ppt2004_01
```

This is how I need each data frame to be structured, but I want to find a faster way to create the whole set of month-year combinations
```{r list of data frames split by month-year, echo=TRUE, eval=F}
# Split `monthly` dataframe with year-month-location on the month and year to create a list of data frames with the total precipitation and location id for each month and year combo
df_list <- split(monthly, list(monthly$year, monthly$month))# 132 data frames
```

I wasn't sure what to do from here in trying to put each data frame into the global environment. I finally hit on the right search terms ("unlist data frame R") revealing this function in an answer on [Stack Overflow](http://stackoverflow.com/questions/17697239/r-unlist-a-list-of-dataframes):

```{r unlist data frames into environment, eval=FALSE}
list2env(df_list, .GlobalEnv))
```

Trying to figure out a `plyr` approach with `assign` so that I can name the data frames, but not having much luck.

```{r plyr unlist (fail), echo=FALSE, eval=FALSE}
# ldply(df_list, function(i){
#       assign(df_list[i], df_list[[i]])
# })
```

Two commenters asked why you would even want to do this when you could just work on them as elements of the list. Can I match up the spatial data to the data frames in the list? Then can I also pass each data frame in that list to an interpolation function?

I am leaving the data frames in the list for now.

I was working on trying to subset the named list of dataframes using wildcards/regular expressions (`regexp`). I wanted to break them up by year due to relocation of some rain gauges, so this is necessary to make sure the data is joined to the correct coordinates. Rain gauges typically were not moved very far, so it may not matter much, but I am being thorough.

I figured out how to do it using regular expressions.

```{r subset list using regular expression, echo=F, eval=F}
# These first two lines didn't get me anywhere
# l <- subset(df_list, df_list["*"]<=df_list["*2008"])
# Filter(function(x) length(x) > 3, l)

# I want to match on 200* EXCEPT 2009*, so the expression is matching on strings with anything following 200 except skipping the string if the next character is a 9.
df_list_names <- names(df_list)
l_names <- df_list_names[grep("200[^9]", df_list_names)]# Testing syntax
df_list_0408 <- df_list[grep("200[^9]", df_list_names)]# Making subsetted list

# I want to get the 2009-2011 data frames. The `|` operates as "or" in the expression, so I am matching on either of these three strings
df_list_0911 <- df_list[grep("2009|2010|2011", df_list_names)]# Making subsetted list

# And now the 2012-2014 data frames
l_names <- df_list_names[grep("2012|2013|2014", df_list_names)]
df_list_1214 <- df_list[grep("2012|2013|2014", df_list_names)]
```


A couple of rain gauges have been moved to new locations near their established areas over the years, so I have shapefiles corresponding the move of one or more gauge stations.

```{r list rain gague shapefiles}
list.files("shapefiles", pattern = "*.shp")
```

This shows that there are four shapefiles for various stretches of the study, 2003-2008, 2009-2011, 2012-2014, and then 2015_2016. The 2015 is the result of moving a rain gauge during our 2014 visits. I will not be using that file for the data I have in hand.

Develop variables of rainfall for each rainy season: number of days with rain `rainXX_dys`, average daily rainfall `rainXX_avg`, and total rainfall `rainXX_tot`.

```{r breakdown daily into seasonal aggregation, message=FALSE}
# library(dplyr)
summary(filter(dy_rg_data, stationid == "glenellen"))
length(unique(dy_rg_data$stationid))# 25 rain gauge stations in total
dy_rg_data$stationid <- as.factor(dy_rg_data$stationid)

# Calculate number of days, average, total with recorded rain

## rs = rainy season, November 1 - May 31
## ss = sample season, November 1 - April 30

summary(dy_rg_data %>% filter(date >= "2003-11-01", date <= "2004-05-31"))
dys_rain <- dy_rg_data %>% 
      filter(date >= "2003-11-01", date <= "2004-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>% 
      summarise(rain04_dys_rs = length(daily_ppt), 
                rain04_avg_rs = sum(daily_ppt)/212, 
                rain04_tot_rs = sum(daily_ppt))

dys_rain <- merge(dys_rain,
                  dy_rg_data %>% 
      filter(date >= "2003-11-01", date <= "2004-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>% 
      summarise(rain04_dys_ss = length(daily_ppt), 
                rain04_avg_ss = sum(daily_ppt)/181, 
                rain04_tot_ss = sum(daily_ppt)),
      by = "stationid", all = T)
summary(dys_rain)


summary(dy_rg_data %>% filter(date >= "2004-11-01", date <= "2005-05-31"))
na.2005 <- dy_rg_data %>% filter(date >= "2004-11-01", date <= "2005-05-31", is.na(daily_ppt))
summary(na.2005)# indicates FOP will be questionable w/29 missing values
rm(na.2005)
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2004-11-01", date <= "2005-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain05_dys_rs = length(daily_ppt), 
                rain05_avg_rs = sum(daily_ppt)/211, 
                rain05_tot_rs = sum(daily_ppt)), 
      by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2004-11-01", date <= "2005-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain05_dys_ss = length(daily_ppt), 
                rain05_avg_ss = sum(daily_ppt)/180, 
                rain05_tot_ss = sum(daily_ppt)), 
      by = "stationid", all = T)


summary(dy_rg_data %>% filter(date >= "2005-11-01", date <= "2006-05-31"))
na.2006 <- dy_rg_data %>% filter(date >= "2005-11-01", date <= "2006-05-31", is.na(daily_ppt))
summary(na.2006)# FOP questionable w/44 missing values
rm(na.2006)
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2005-11-01", date <= "2006-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain06_dys_rs = length(daily_ppt), 
                rain06_avg_rs = sum(daily_ppt)/211, 
                rain06_tot_rs = sum(daily_ppt)), 
      by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2005-11-01", date <= "2006-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain06_dys_ss = length(daily_ppt), 
                rain06_avg_ss = sum(daily_ppt)/180, 
                rain06_tot_ss = sum(daily_ppt)), 
      by = "stationid", all = T)

      
summary(dy_rg_data %>% filter(date >= "2006-11-01", date <= "2007-05-31"))
na.2007 <- dy_rg_data %>% filter(date >= "2006-11-01", date <= "2007-05-31", is.na(daily_ppt))
summary(na.2007)# St. Helena station has 40 missing values
rm(na.2007)
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2006-11-01", date <= "2007-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain07_dys_rs = length(daily_ppt), 
                rain07_avg_rs = sum(daily_ppt)/211, 
                rain07_tot_rs = sum(daily_ppt)), 
      by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2006-11-01", date <= "2007-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain07_dys_ss = length(daily_ppt), 
                rain07_avg_ss = sum(daily_ppt)/180, 
                rain07_tot_ss = sum(daily_ppt)), 
      by = "stationid", all = T)

      
summary(dy_rg_data %>% filter(date >= "2007-11-01", date <= "2008-05-31"))
dy_rg_data %>% filter(date >= "2007-11-01", date <= "2008-05-31", is.na(daily_ppt))
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2007-11-01", date <= "2008-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain08_dys_rs = length(daily_ppt), 
                rain08_avg_rs = sum(daily_ppt)/212, 
                rain08_tot_rs = sum(daily_ppt)), by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2007-11-01", date <= "2008-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain08_dys_ss = length(daily_ppt), 
                rain08_avg_ss = sum(daily_ppt)/181, 
                rain08_tot_ss = sum(daily_ppt)), by = "stationid", all = T)


summary(dy_rg_data %>% filter(date >= "2008-11-01", date <= "2009-05-31"))
dy_rg_data %>% filter(date >= "2008-11-01", date <= "2009-05-31", is.na(daily_ppt))# Glen Ellen WNW station has 10 missing values, 7 in April
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2008-11-01", date <= "2009-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain09_dys_rs = length(daily_ppt), 
                rain09_avg_rs = sum(daily_ppt)/211, 
                rain09_tot_rs = sum(daily_ppt)), by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2008-11-01", date <= "2009-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain09_dys_ss = length(daily_ppt), 
                rain09_avg_ss = sum(daily_ppt)/180, 
                rain09_tot_ss = sum(daily_ppt)), by = "stationid", all = T)

      
summary(dy_rg_data %>% filter(date >= "2009-11-01", date <= "2010-05-31"))
na.2010 <- dy_rg_data %>% filter(date >= "2009-11-01", date <= "2010-05-31", is.na(daily_ppt))
summary(na.2010)# Glen Ellen WNW station has 9 missing values
rm(na.2010)
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2009-11-01", date <= "2010-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain10_dys_rs = length(daily_ppt), 
                rain10_avg_rs = sum(daily_ppt)/211, 
                rain10_tot_rs = sum(daily_ppt)), 
      by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2009-11-01", date <= "2010-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain10_dys_ss = length(daily_ppt), 
                rain10_avg_ss = sum(daily_ppt)/180, 
                rain10_tot_ss = sum(daily_ppt)), 
      by = "stationid", all = T)


summary(dy_rg_data %>% filter(date >= "2010-11-01", date <= "2011-05-31"))
na.2011 <- dy_rg_data %>% filter(date >= "2010-11-01", date <= "2011-05-31", is.na(daily_ppt))
summary(na.2011)# Sonoma1 station has 83 missing values; excl. from interp. 
filter(na.2011, stationid == "sonoma1")# mostly after February
rm(na.2011)
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2010-11-01", date <= "2011-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>% 
      summarise(rain11_dys_rs = length(daily_ppt), 
                rain11_avg_rs = sum(daily_ppt)/211, 
                rain11_tot_rs = sum(daily_ppt)), 
      by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2010-11-01", date <= "2011-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>% 
      summarise(rain11_dys_ss = length(daily_ppt), 
                rain11_avg_ss = sum(daily_ppt)/180, 
                rain11_tot_ss = sum(daily_ppt)), 
      by = "stationid", all = T)

      
summary(dy_rg_data %>% filter(date >= "2011-11-01", date <= "2012-05-31"))
na.2012 <- dy_rg_data %>% filter(date >= "2011-11-01", date <= "2012-05-31", is.na(daily_ppt))
summary(na.2012)# Sonoma1 station has 166 missing, Glen Ellen WNW 14 missing
filter(na.2012, stationid == "sonoma1")# starts in November, should exclude
rm(na.2012)
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2011-11-01", date <= "2012-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>% 
      summarise(rain12_dys_rs = length(daily_ppt), 
                rain12_avg_rs = sum(daily_ppt)/212, 
                rain12_tot_rs = sum(daily_ppt)), 
      by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2011-11-01", date <= "2012-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>% 
      summarise(rain12_dys_ss = length(daily_ppt), 
                rain12_avg_ss = sum(daily_ppt)/181, 
                rain12_tot_ss = sum(daily_ppt)), 
      by = "stationid", all = T)


summary(dy_rg_data %>% filter(date >= "2012-11-01", date <= "2013-05-31"))
dy_rg_data %>% filter(date >= "2012-11-01", date <= "2013-05-31", is.na(daily_ppt))
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2012-11-01", date <= "2013-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain13_dys_rs = length(daily_ppt), 
                rain13_avg_rs = sum(daily_ppt)/211, 
                rain13_tot_rs = sum(daily_ppt)), 
      by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2012-11-01", date <= "2013-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain13_dys_ss = length(daily_ppt), 
                rain13_avg_ss = sum(daily_ppt)/180, 
                rain13_tot_ss = sum(daily_ppt)), 
      by = "stationid", all = T)

      
summary(dy_rg_data %>% filter(date >= "2013-11-01", date <= "2014-05-31"))
dy_rg_data %>% filter(date >= "2013-11-01", date <= "2014-05-31", is.na(daily_ppt))# both Glen Ellen stations missing data, WNW 11 mostly in March, the other 6
dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2013-11-01", date <= "2014-05-31", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain14_dys_rs = length(daily_ppt), 
                rain14_avg_rs = sum(daily_ppt)/211, 
                rain14_tot_rs = sum(daily_ppt)), 
      by = "stationid", all = T)

dys_rain <- merge(dys_rain, dy_rg_data %>% 
      filter(date >= "2013-11-01", date <= "2014-04-30", daily_ppt > 0) %>% 
      group_by(stationid) %>%
      summarise(rain14_dys_ss = length(daily_ppt), 
                rain14_avg_ss = sum(daily_ppt)/180, 
                rain14_tot_ss = sum(daily_ppt)), 
      by = "stationid", all = T)

summary(dys_rain)

sample_season_rain <- select(dys_rain, stationid, ends_with("ss"))
rainy_season_rain <- select(dys_rain, stationid, ends_with("rs"))
```

I think that the `NAs` during some seasons are due to rain gauges not recording, recording bad data, or being decommissioned.

```{r investigate dys_rain NAs}
filter(dys_rain, is.na(rain04_dys_rs)) # mackie, glenellen x2, sonoma2, wunderground stations x2 (6)
filter(dys_rain, is.na(rain06_dys_rs)) # kunde, mitsui, sugarloaf, glenellen x2, sonoma2, wunderground stations x2 (8)
filter(dys_rain, is.na(rain07_dys_rs)) # fop, kunde, spaulding, glenellen x2, sonoma2, wunderground stations x2 (8)
filter(dys_rain, is.na(rain08_dys_rs)) # fop, glenellen x2, sonoma2, wunderground stations x2 (6)
filter(dys_rain, is.na(rain09_dys_rs)) # backman, cook, fop, mitsui, wunderground stations x2 (6)
filter(dys_rain, is.na(rain10_dys_rs)) # fop, sugarloaf, kcasanta225 (3)
filter(dys_rain, is.na(rain11_dys_rs)) # beltane, fop, mitsui, sugarloaf, votruba, kcasanta225 (6)
filter(dys_rain, is.na(rain12_dys_rs)) # annadel, backman, beltane, fop, kunde, mitsui, sugarloaf, votruba, kcasanta225 (9)
filter(dys_rain, is.na(rain14_dys_rs)) # annadel, badger, beltane, cook, fop, sawi, secret, spaulding, sugarloaf, votruba (10)
```

I noticed that the way the series of join statements were written I missed the "mackie" rain gauge because it was not part of the 2004 season. I corrected this and another join issue that arose later with adding the other rain gauges by calling the `merge` function with `all = TRUE` option. This is a "full outer join" that retains all the values for all the rows, inserting `NAs` for columns where the data is missing for that row. Here the rows correspond to the rain gauge station name.

I still need to investigate the very high maximum total rainfall for 2005. (From previously playing with this data I think I may find some other issues as well).

```{r investigate dys_rain extreme values}
(filter(dys_rain, rain05_tot_rs > 70))
summary(select(dys_rain, stationid, rain05_tot_rs))
sd(filter(dys_rain, rain05_tot_rs < 70)$rain05_tot_rs, na.rm = T)
# excluding two major outliers sd ≈ 8.6, 3 x sd ≈ 26, mean ≈ 32, max ≈ 49
# the total around 70 for Beltane would fall just within 3 standard deviations of the maximum value, which was recorded by the St. Helena station located at a much higher elevation.
ggplot(data = dys_rain, aes(x = stationid, y = rain05_tot_rs))+
      geom_point() +
      geom_abline(intercept = mean(dys_rain$rain05_tot_rs, na.rm = T), slope = 0)
```

I think the 2005 seasonal totals for Beltane and FOP are far too big compared to the rest of the measurements to be considered reliable. Particular months may be okay.

```{r monthly data 2003-2008, echo=F, eval=F}
month_rg_2003_2008 <- dy_rg_data %>%
      group_by(id, year, month) %>% 
      filter(year<=2008) %>%
      summarise(monthly_ppt = sum(daily_ppt))
month_rg_2003_2008$id <- as.factor(month_rg_2003_2008$id)
summary(month_rg_2003_2008)

day_rg_2003_2008 <- dy_rg_data %>%
      group_by(year, month, day, id) %>%
      filter(year<=2008)
summary(day_rg_2003_2008)
```

```{r read in shapefile(s) and DEM, messages=F, echo=F}
# library(sp)
library(rgdal, verbose = F)

rg_2003_2008 <- readOGR(dsn="shapefiles", layer="RainGauges2003_2008")
rg0308 <- rg_2003_2008[,3:5]
# str(rg0308)
rg0308@data$NAME
unique(dys_rain$stationid)
rg0308@data$NAME <- tolower(rg0308@data$NAME)
rg0308@data$NAME[rg0308@data$NAME=="macki"] <- "mackie"
rg0308@data$NAME[rg0308@data$NAME=="sdc"] <- "sec"
rg0308@data$NAME[rg0308@data$NAME=="sugar"] <- "sugarloaf"
unique(rg0308@data$NAME)
rg0308@data <- rename(rg0308@data, stationid = NAME, lat = NORTHING, lon = EASTING)
plot(rg0308)
# rg_2003_2008@coords <- rg_2003_2008@coords[-7,] # remove coordinates for FOP
# plot(rg_2003_2008)
# rg_2003_2008@data <- filter(rg_2003_2008@data, NAME != "fop")

rg_2009_2011 <- readOGR(dsn = "shapefiles/", layer = "RainGauges2009_2011")
names(rg_2009_2011)
# str(rg_2009_2011)
rg0911 <- rg_2009_2011[,3:5]
rg0911$NAME
rg0911$NAME <- tolower(rg0911$NAME)
rg0911@data$NAME[rg0911@data$NAME=="macki"] <- "mackie"
rg0911@data$NAME[rg0911@data$NAME=="sdc"] <- "sec"
rg0911@data$NAME[rg0911@data$NAME=="sugar"] <- "sugarloaf"
rg0911@data <- rename(rg0911@data, stationid = NAME, lat = NORTHING, lon = EASTING)

rg_2012_2014 <- readOGR(dsn = "shapefiles/", layer = "RainGauges2012_2014")
rg1214 <- rg_2012_2014[,2:4]
# str(rg_2012_2014)
rg1214$NAME
rg1214$NAME <- tolower(rg1214$NAME)
rg1214@data$NAME[rg1214@data$NAME=="macki"] <- "mackie"
rg1214@data$NAME[rg1214@data$NAME=="sdc"] <- "sec"
rg1214@data$NAME[rg1214@data$NAME=="sugar"] <- "sugarloaf"
rg1214@data <- rename(rg1214@data, stationid = NAME, lat = NORTHING, lon = EASTING)

glen_ellen_rg <- readOGR("shapefiles/", "GlenEllen_ppt_2009_2015")
summary(glen_ellen_rg)
glen_ellen_rg$station <- tolower(glen_ellen_rg$station)
glen_ellen_rg@data <- rename(glen_ellen_rg@data, stationid = station)

# add_rg <- readOGR("shapefiles/", "cdec_noaa_wundergrnd_rgloc")
# summary(add_rg)
# add_rg <- add_rg[add_rg$stationid != "kcasaint5" & add_rg$stationid != "msthc1" & add_rg$stationid != "kcanapa8",]

other_rg <- readOGR("shapefiles/", "otherRGs")
summary(other_rg)
plot(other_rg)

# add_rg@data$stationid
other_rg@data$stationid
other_rg@data <- rename(other_rg@data, lat = lat_dd, lon = lon_dd)
other_rg@data <- select(other_rg@data, stationid, lat, lon)

# single "other" rain gauge shapefiles
sonoma_rgs <- readOGR("shapefiles/", "sonoma_rgs")
lichau_rg <- readOGR("shapefiles/", "lichau_rg")
petaluma_rg <- readOGR("shapefiles/", "petalumaE_rg")
sh4 <- readOGR("shapefiles/", "sthelena_rg")
santa_rg <- readOGR("shapefiles/", "santa225_rg")
diamonda_rg <- readOGR("shapefiles/", "diamonda_rg")

# summary(diamonda_daily)# 2010-01-01 to 2014-05-31
ggplot(data = filter(dy_rg_data_tmp, stationid == "kcasonom8"), aes(x = date, y = daily_ppt)) +
      geom_point() +
      ggtitle("Diamond A (?) Rain Gauge Data")
      
# summary(lichau_daily)# 2003-11-01 to 2014-05-31
ggplot(data = filter(dy_rg_data_tmp, stationid == "lichau"), aes(x = date, y = daily_ppt)) +
      geom_point() +
      ggtitle("Lichau Rain Gauge Data")

# summary(petaluma_daily)# 2003-11-01 to 2015-05-31
ggplot(data = filter(dy_rg_data_tmp, stationid == "petaluma_e"), aes(x = date, y = daily_ppt)) +
      geom_point() +
      ggtitle("Petaluma East Rain Gauge Data")

# summary(santa225_daily)# 2013-01-01 to 2014-05-31, good for 2014
ggplot(data = filter(dy_rg_data_tmp, stationid == "kcasanta225"), aes(x = date, y = daily_ppt)) +
      geom_point() +
      ggtitle("Santa Rosa 225 Rain Gauge Data")

# summary(sonoma1_daily)# 2003-11-01 to 2014-05-31
ggplot(data = filter(dy_rg_data_tmp, stationid == "sonoma1"), aes(x = date, y = daily_ppt)) +
      geom_point() +
      ggtitle("Sonoma 1 Rain Gauge Data")

# summary(sonoma2_daily)# 2009-02-11 to 2014-05-09
ggplot(data = filter(dy_rg_data_tmp, stationid == "sonoma2"), aes(x = date, y = daily_ppt)) +
      geom_point() +
      ggtitle("Sonoma 2 Rain Gauge Data")

# summary(sthelena_daily)# 2003-11-01 to 2015-10-14
ggplot(data = filter(dy_rg_data_tmp, stationid == "sh4wsw"), aes(x = date, y = daily_ppt)) +
      geom_point() +
      ggtitle("St. Helena WSW Rain Gauge Data")

# library(raster)
# Load elevation raster and transform shapefiles to match projection
elev <- raster("dem/sod15m.tif")
str(elev)
crs <- elev@crs
rg0308 <- spTransform(rg0308, CRS=crs)
rg0911 <- spTransform(rg0911, CRS=crs)
rg1214 <- spTransform(rg1214, CRS=crs)
glen_ellen_rg <- spTransform(glen_ellen_rg, CRS=crs)
other_rg <- spTransform(other_rg, CRS=crs)

all_rg <- rbind(rg0308, rg0911, rg1214, glen_ellen_rg, other_rg)
summary(all_rg)
all_rg <- all_rg[,1]
all_rg@data$stationid
all_rg <- remove.duplicates(all_rg)
plot(all_rg)
text(all_rg, labels = all_rg@data$stationid)
```

```{r append other rain gauges}
glen_ellen_rg
other_rg
other_rg <- rbind(other_rg, glen_ellen_rg)

rg0308 <- rbind(rg0308, other_rg)
rg0911 <- rbind(rg0911, other_rg)
rg1214 <- rbind(rg1214, other_rg)

rg0308@coords
rg0911@coords
rg1214@coords

# Drop lat and lon from data attributes
rg0308@data <- select(rg0308@data, stationid)
rg0911@data <- select(rg0911@data, stationid)
rg1214@data <- select(rg1214@data, stationid)
```


```{r quick visual of rain gauge locations}
# Overlay rain gauge files on elevation grid. Elevation is in feet.
plot(elev)
plot(rg0308, pch = 25, col = "blue", bg="transparent", add=TRUE)
#spplot(elev, scales=list(draw=T), sp.layout=list("sp.points", rg_2003_2008))
plot(rg0911, pch = 20, col = "red", add = T)
plot(rg1214, pch = 2, add = T)
plot(glen_ellen_rg, add = T)
plot(other_rg, add = T)
text(other_rg, labels = other_rg@data$stationid, pos = 3)

# writeOGR(all_rg, "shapefiles/", "all_rgs", driver = "ESRI Shapefile", overwrite_layer = T)
```

Based on visualizing the DEM and the "other" rain gauges in QGIS the 'sonoma2' and 'petaluma_e' stations are both outside the boundary. I think that I could still use these for a 2-D spline interpolation, but they would be inappropriate for a 3-D interpolation that is dependent on the local DEM I am currently using. (I could use a different DEM that is cropped or masked to a slightly larger area)

Extracting elevation values to the rain gauge point locations
```{r extract elevation to points}
# library(raster)
rg0308@data$elev_ft <- extract(elev, rg0308)
rg0911@data$elev_ft <- extract(elev, rg0911)
rg1214@data$elev_ft <- extract(elev, rg1214)
```

Joining seasonal precipitation data to spatial points data frame
```{r joining data}
# detach("package:raster", unload=TRUE) # unmask `select` in `dplyr`
summary(dys_rain)

# inner_join(rg_2003_2008@data, dys_rain %>% select(-starts_with("rain1"), -contains("09")), by = c("NAME" = "plotid"))

# all_rg@data <- inner_join(all_rg@data, dys_rain)

rg0308@data <- inner_join(rg0308@data, dys_rain %>% select(-starts_with("rain1"), -contains("09")), by = "stationid")
rg0308@data$elev_m <- rg0308@data$elev_ft * .305
summary(rg0308)

rg0911@data <- inner_join(rg0911@data, dys_rain %>% select(stationid, contains("09"), contains("10"), contains("11")), by = "stationid")
rg0911@data$elev_m <- rg0911@data$elev_ft * .305
summary(rg0911)

rg1214@data <- inner_join(rg1214@data, dys_rain %>% select(stationid, contains("12"), contains("13"), contains("14")), by = "stationid")
rg1214@data$elev_m <- rg1214@data$elev_ft * .305
summary(rg1214)
```

Now I am going to create new datasets that are for each season
```{r filter data to each season}
# Transform projections to NAD83 UTM Zone 10 to match the GRASS project location
soco_loc <- readOGR("shapefiles/", "rg05_vpolys", verbose = F)
rg0308 <- spTransform(rg0308, CRSobj = proj4string(soco_loc))
rg0911 <- spTransform(rg0911, CRSobj = proj4string(soco_loc))
rg1214 <- spTransform(rg1214, CRSobj = proj4string(soco_loc))

writeOGR(rg0308, dsn = "shapefiles/", layer = "rgs_0308", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rg0911, dsn = "shapefiles/", layer = "rgs_0911", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rg1214, dsn = "shapefiles/", layer = "rgs_1214", driver = "ESRI Shapefile", overwrite_layer = T)

rainy04 <- rg0308[!is.na(rg0308$rain04_dys_rs),]# remove NAs for 2004 season
rainy04@data <- rainy04@data %>% select(stationid, elev_ft, elev_m, starts_with("rain04"))
rainy04@data$ppt_mm_rs <- rainy04@data$rain04_tot_rs * 25.4
rainy04@data$ppt_mm_ss <- rainy04@data$rain04_tot_ss * 25.4
rainy04; summary(rainy04)

# None missing in 2005, but removed "beltane" (row 4) and "fop" (row 7) because severe positive outliers, recording > 70 and 109 inches of rain. The next highest was 44 inches
rainy05 <- rg0308[!is.na(rg0308$rain05_dys_rs), c("stationid", "rain05_dys_rs", "rain05_avg_rs", "rain05_tot_rs", "rain05_dys_ss", "rain05_avg_ss", "rain05_tot_ss", "elev_m")]
rainy05@data
rainy05 <- rainy05[rainy05$stationid != "beltane" & rainy05$stationid != "fop",]# remove outlier stations FOP and Beltane
rainy05@data$ppt_mm_rs <- rainy05@data$rain05_tot_rs * 25.4
rainy05@data$ppt_mm_ss <- rainy05@data$rain05_tot_ss * 25.4
rainy05@data; summary(rainy05)

rainy06 <- rg0308[!is.na(rg0308$rain06_dys_rs) & !is.na(rg0308$rain06_dys_ss), c("stationid", "rain06_dys_rs", "rain06_avg_rs", "rain06_tot_rs", "rain06_dys_ss", "rain06_avg_ss", "rain06_tot_ss", "elev_m")]
rainy06@data # In 2006 there were some NAs and also some very small values (<2 inches) that seem extremely unreliable compared to the other measurements.
rainy06 <- rainy06[rainy06$stationid == "cook" | rainy06$stationid == "mackie" | rainy06$stationid == "spaulding" | rainy06$stationid == "votruba" | rainy06$stationid == "yahng" | rainy06$stationid == "lichau" | rainy06$stationid == "petaluma_e" | rainy06$stationid == "sonoma1" | rainy06$stationid == "sh4wsw",]
rainy06@data$ppt_mm_rs <- rainy06@data$rain06_tot_rs * 25.4
rainy06@data$ppt_mm_ss <- rainy06@data$rain06_tot_ss * 25.4
rainy06@data; summary(rainy06); plot(rainy06)
# This results in essentially zero coverage in the southeastern part of the study area

rainy07 <- rg0308[!is.na(rg0308$rain07_dys_rs) ,c("stationid", "rain07_dys_rs", "rain07_avg_rs", "rain07_tot_rs", "rain07_dys_ss", "rain07_avg_ss", "rain07_tot_ss", "elev_m")]
rainy07@data # Again some values that don't seem very reliable
rainy07 <- rainy07[rainy07$stationid != "mitsui" & rainy07$stationid != "cook" & rainy07$stationid != "secret" & rainy07$stationid != "mackie" &  rainy07$stationid != "sugarloaf" & rainy07$stationid != "votruba" & rainy07$stationid != "yahng",]
rainy07@data$ppt_mm_rs <- rainy07@data$rain07_tot_rs * 25.4
rainy07@data$ppt_mm_ss <- rainy07@data$rain07_tot_ss * 25.4
rainy07@data; summary(rainy07); plot(rainy07)

rainy08 <- rg0308[!is.na(rg0308$rain08_dys_rs), c("stationid", "rain08_dys_rs", "rain08_avg_rs", "rain08_tot_rs", "rain08_dys_ss", "rain08_avg_ss", "rain08_tot_ss", "elev_m")]
rainy08@data
# A few unreliable values for total rainfall
rainy08 <- rainy08[rainy08$stationid != "backman" & rainy08$stationid != "badger" & rainy08$stationid != "kunde" & rainy08$stationid != "spaulding",]
rainy08@data$ppt_mm_rs <- rainy08@data$rain08_tot_rs * 25.4
rainy08@data$ppt_mm_ss <- rainy08@data$rain08_tot_ss * 25.4
rainy08@data; summary(rainy08); plot(rainy08)

rainy09 <- rg0911[!is.na(rg0911$rain09_dys_rs), c("stationid", "rain09_dys_rs", "rain09_avg_rs", "rain09_tot_rs", "rain09_dys_ss", "rain09_avg_ss", "rain09_tot_ss", "elev_m")]
rainy09@data
# Remove a few unreliable values
rainy09 <- rainy09[rainy09$stationid != "annadel" & rainy09$stationid != "beltane" & rainy09$stationid != "secret" & rainy09$stationid != "sec" & rainy09$stationid != "glenellen" & rainy09$stationid != "glenellenwnw",]
rainy09@data$ppt_mm_rs <- rainy09@data$rain09_tot_rs * 25.4
rainy09@data$ppt_mm_ss <- rainy09@data$rain09_tot_ss * 25.4
rainy09@data; summary(rainy09); plot(rainy09)

rainy10 <- rg0911[!is.na(rg0911$rain10_dys_rs), c("stationid", "rain10_dys_rs", "rain10_avg_rs", "rain10_tot_rs", "rain10_dys_ss", "rain10_avg_ss", "rain10_tot_ss", "elev_m")]
rainy10@data
rainy10 <- rainy10[rainy10$stationid != "annadel" & rainy10$stationid != "backman" & rainy10$stationid != "badger" & rainy10$stationid != "mitsui" & rainy10$stationid != "sec" & rainy10$stationid != "spaulding",]
rainy10@data$ppt_mm_rs <- rainy10@data$rain10_tot_rs * 25.4
rainy10@data$ppt_mm_ss <- rainy10@data$rain10_tot_ss * 25.4
rainy10@data; summary(rainy10); plot(rainy10)

rainy11 <- rg0911[!is.na(rg0911$rain11_dys_rs), c("stationid", "rain11_dys_rs", "rain11_avg_rs", "rain11_tot_rs", "rain11_dys_ss", "rain11_avg_ss", "rain11_tot_ss", "elev_m")]
rainy11@data
rainy11 <- rainy11[rainy11$stationid != "yahng",]
rainy11@data$ppt_mm_rs <- rainy11@data$rain11_tot_rs * 25.4
rainy11@data$ppt_mm_ss <- rainy11@data$rain11_tot_ss * 25.4
rainy11@data; summary(rainy11); plot(rainy11)

rainy12 <- rg1214[!is.na(rg1214$rain12_dys_rs), c("stationid", "rain12_dys_rs", "rain12_avg_rs", "rain12_tot_rs", "rain12_dys_ss", "rain12_avg_ss", "rain12_tot_ss", "elev_m")]
rainy12@data
rainy12 <- rainy12[rainy12$stationid != "cook" & rainy12$stationid != "secret" & rainy12$stationid != "spaulding" & rainy12$stationid != "sec",]
rainy12@data$ppt_mm_rs <- rainy12@data$rain12_tot_rs * 25.4
rainy12@data$ppt_mm_ss <- rainy12@data$rain12_tot_ss * 25.4
rainy12@data; summary(rainy12); plot(rainy12)

rainy13 <- rg1214[!is.na(rg1214$rain13_dys_rs), c("stationid", "rain13_dys_rs", "rain13_avg_rs", "rain13_tot_rs", "rain13_dys_ss", "rain13_avg_ss", "rain13_tot_ss", "elev_m")]
rainy13@data
rainy13 <- rainy13[rainy13$stationid != "mitsui" & rainy13$stationid != "mackie" & rainy13$stationid != "kcasanta225",]
rainy13@data$ppt_mm_rs <- rainy13@data$rain13_tot_rs * 25.4
rainy13@data$ppt_mm_ss <- rainy13@data$rain13_tot_ss * 25.4
rainy13@data; summary(rainy13); plot(rainy13)

rainy14 <- rg1214[!is.na(rg1214$rain14_dys_rs), c("stationid", "rain14_dys_rs", "rain14_avg_rs", "rain14_tot_rs", "rain14_dys_ss", "rain14_avg_ss", "rain14_tot_ss", "elev_m")]
rainy14@data
rainy14 <- rainy14[rainy14$stationid != "mitsui" & rainy14$stationid != "kunde" & rainy14$stationid != "yahng",]
rainy14@data$ppt_mm_rs <- rainy14@data$rain14_tot_rs * 25.4
rainy14@data$ppt_mm_ss <- rainy14@data$rain14_tot_ss * 25.4
rainy14@data; summary(rainy14); plot(rainy14)


writeOGR(rainy04, dsn = "shapefiles/", layer = "rain_gauges04", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy05, dsn = "shapefiles/", layer = "rain_gauges05", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy06, dsn = "shapefiles/", layer = "rain_gauges06", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy07, dsn = "shapefiles/", layer = "rain_gauges07", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy08, dsn = "shapefiles/", layer = "rain_gauges08", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy09, dsn = "shapefiles/", layer = "rain_gauges09", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy10, dsn = "shapefiles/", layer = "rain_gauges10", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy11, dsn = "shapefiles/", layer = "rain_gauges11", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy12, dsn = "shapefiles/", layer = "rain_gauges12", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy13, dsn = "shapefiles/", layer = "rain_gauges13", driver = "ESRI Shapefile", overwrite_layer = T)
writeOGR(rainy14, dsn = "shapefiles/", layer = "rain_gauges14", driver = "ESRI Shapefile", overwrite_layer = T)

save.image("~/Documents/soco_ppt/analysis201602.RData")
```

At this point these shapefiles need to be imported to GRASS for doing the precipitation interpolation.

#### Pairs plots and linear models of seasonally aggregated data
```{r read in 'rain_gaugesXX' shapefiles}
library(rgdal)
shp_list <- list.files("shapefiles", pattern = "^rain_gauges")
shp_list <- grep(".shp$", shp_list, value = TRUE)

shp_list <- strsplit(shp_list, ".shp")
tmp <- strsplit(unlist(shp_list), "rain_gauges")
tmp <- unlist(tmp)
tmp <- tmp[seq(2, 22, 2)]

n = 4
for(shp in shp_list){
      assign(paste("rainy", "0", n, sep = ""), 
             readOGR(dsn = "shapefiles", layer = shp))
      n = n + 1
}
rainy10 <- rainy010; rainy11 <- rainy011 
rainy12 <- rainy012; rainy13 <- rainy013
rainy14 <- rainy014
rm(list = ls(pattern = "01"))
```

```{r pairs plots & linear models, eval=F}
library(MuMIn)
panel.hist <- function(x, ...){
    usr <- par("usr"); on.exit(par(usr))
    par(usr = c(usr[1:2], 0, 1.5) )
    h <- hist(x, plot = FALSE)
    breaks <- h$breaks; nB <- length(breaks)
    y <- h$counts; y <- y/max(y)
    rect(breaks[-nB], 0, breaks[-1], y, col = "cyan", ...)
}
panel.cor <- function(x, y, digits = 2, prefix = "", cex.cor, ...){
    usr <- par("usr"); on.exit(par(usr))
    par(usr = c(0, 1, 0, 1))
    r <- abs(cor(x, y))
    txt <- format(c(r, 0.123456789), digits = digits)[1]
    txt <- paste0(prefix, txt)
    if(missing(cex.cor)) cex.cor <- 0.8/strwidth(txt)
    text(0.5, 0.5, txt, cex = cex.cor * r)
}

pairs(rainy04@data %>% select(-station, -elev_ft), lower.panel = panel.cor, diag.panel = panel.hist, main = "2004 Season") # strongest relationship between lon & total rainfall (0.63); total rainfall looks maybe slightly right skewed and is about equally correlated with lat & elev

lm04a <- lm(rain04_tot ~ lon + lat + elev_m, data = rainy04@data, na.action = "na.fail")
summary(lm04a)
par(mfrow = c(2,2)); plot(lm04a)
lm04sel <- dredge(lm04a, extra =  list("*" = function(x){
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm04sel
# Including elevation improves adjusted R-squared by about 9% and essentially has no change to AICc (0.4)

pairs(rainy05@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2005 Season") # strongest relationship between lat & total rainfall (0.61); of note: lat & rainy days (0.70), lon & average rainfall(0.66)
lm05a <- lm(rain05_tot ~ lat + elev_m + lon, data = rainy05@data, na.action = "na.fail")
summary(lm05a)
par(mfrow = c(2,2)); plot(lm05a)
lm05sel <- dredge(lm05a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm05sel
# Best explanatory power and AICc with just latitude for this year

pairs(rainy06@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2006 Season") # strongest relationship between lat & total rainfall (0.68), lon & total rainfall (0.67), elev & total rainfall (0.29); lat & lon (0.66), but I think this is because of the limited coverage being clustered to the NW quadrant of study area. Distributions are pretty uniform and there is not much variation within the measurements for this much reduced data.
lm06a <- lm(rain06_tot ~ lat + lon + elev_m, data = rainy06@data, na.action = "na.fail")
summary(lm06a)
par(mfrow = c(2,2)); plot(lm06a) # These diagnostic plots look terrible
lm06sel <- dredge(lm06a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm06sel
# 

pairs(rainy07@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2007 Season") # strongest relationship between elev & total rainfall (0.52), though total rainfall looks like it has some negative skew;
lm07a <- lm(rain07_tot^4 ~ lat + lon + elev_m, data = rainy07@data, na.action = "na.fail")
summary(lm07a)
par(mfrow = c(2,2)); plot(lm07a) # Poor fitting model with adjusted r-squared negative
lm07sel <- dredge(lm07a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm07sel
# Best fit of a set of poor choices is elevation as only predictor. Transforming total rainfall by raising to the 4th power improves r-squared, but does not improve on the null model when AICc is compared (it's actually a little worse than the null model).

pairs(rainy08@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2008 Season") # strongest relationship between lat & total rainfall (0.33)
lm08a <- lm(rain08_tot ~ lat + lon + elev_m, data = rainy08@data, na.action = "na.fail")
summary(lm08a)
par(mfrow = c(2,2)); plot(lm08a) # Poor model with adjusted r-squared negative
lm08sel <- dredge(lm08a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm08sel
# Latitude is best predictor of set of crappy models, but not better than the null model, i.e. no better than the average

pairs(rainy09@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2009 Season") # weak correlation between total rainfall & lat/lon
lm09a <- lm(rain09_tot ~ lat + lon + elev_m, data = rainy09@data, na.action = "na.fail")
summary(lm09a)
par(mfrow = c(2,2)); plot(lm09a) # Poor model with adjusted r-squared negative
lm09sel <- dredge(lm09a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm09sel
# Again, latitude is best from a set of crappy models, but adjusted r-squared is negative, so not better than the null model, i.e. the population average

pairs(rainy10@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2010 Season") # correlation between total rainfall & lat (0.51)
lm10a <- lm(rain10_tot ~ lat + lon + log(elev_m), data = rainy10@data, na.action = "na.fail")
summary(lm10a)
par(mfrow = c(2,2)); plot(lm10a) # Substantial explanatory power in this model
lm10sel <- dredge(lm10a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm10sel
# Best model combined AICc & adj.Rsquared (0.65) has elevation & latitude when elevation is log-transformed

pairs(rainy11@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2011 Season") # Correlation between total rainfall & elevation 0.25
lm11a <- lm(rain11_tot ~ lat + lon + elev_m, data = rainy11@data, na.action = "na.fail")
summary(lm11a)
par(mfrow = c(2,2)); plot(lm11a) # Poor model with adjusted r-squared negative
lm11sel <- dredge(lm11a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm11sel
# Nothing any better than the null model, all adjusted R-squared are negative

pairs(rainy12@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2012 Season") # weak correlation between total rainfall & elevation
lm12a <- lm(rain12_tot ~ lat + lon + elev_m, data = rainy12@data, na.action = "na.fail")
summary(lm12a)
par(mfrow = c(2,2)); plot(lm12a) # Poor model with adjusted r-squared negative
lm12sel <- dredge(lm12a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm12sel
# Nothing any better than the null model, all adjusted R-squared are negative

pairs(rainy14@data %>% select(-station), lower.panel = panel.cor, diag.panel = panel.hist, main = "2014 Season") # correlation between total rainfall & elev (0.70), total rainfall & lon (0.48)
lm14a <- lm(log(rain14_tot) ~ log(elev_m) + lon + lat, data = rainy14@data, na.action = "na.fail")
summary(lm14a)
par(mfrow = c(2,2)); plot(lm14a) # Poor model with small sample size
lm14sel <- dredge(lm14a, extra = list("*" = function(x) {
        s <- summary(x)
        c(Rsq = s$r.squared, adjRsq = s$adj.r.squared,
            F = s$fstatistic[[1]])
    })); lm14sel
# Best adjusted R-squared is with log(elevation), lat, lon (0.61) and total rainfall log-transformed, however, this is sample size of 5 stations. The variation of total rainfall is pretty limited.
```

There are some cases where the data have good coverage and a good fitting model and other seasons where nothing predicts better than the null model. So, I'm not sure if there are some years where I might be able to, or should, try and model a surface. The many factors that influence rainfall measurements over space and time 

  - wind speed and direction (difficult to measure everywhere)
  - gauge placement (human error)
  - elevation/topography
  
Due to the randomness of rainfall over even small areas, the best-practice approach for rainfall modeling is to sample an area as densely as possible and then apply Thiessen polygons. Or, I could try to do a nearest point data extraction for each plot. I think this works for years when there is data from stations throughout the study area (good spatial coverage), but I don't think this would work well when coverage is for only part, or very minimal of the study area. I need to read and understand more about thin plate splines to see if that might be applicable to these data.

(It seems that there is the possibility that these data are bimodal in the precipitation~elevation relationship due to the rainshadowing of the Sonoma Valley and possibly to some extent the Mayacamas by Sonoma Mountain.)


Create Thiessen Polygon surfaces with rainfall values
```{r Thiessen polygons test, eval=F}
# The `voronoi function from the `dismo` package, which was adapted from the code by Carson Farmer
library(dismo)
v1 <- voronoi(rg_2003_2008)
summary(v1)
v1@data$station
(v1@data)
plot(v1, axes = TRUE)

# From jbaums comment here: http://w3facility.org/question/how-to-create-thiessen-polygons-from-points-using-r-packages/
# They link to this site: http://carsonfarmer.com/?p=455, but I can't find the post/place where he wrote the original function.

# Carson's Voronoi polygons function
voronoipolygons <- function(x) {
  library(deldir)
  if (.hasSlot(x, 'coords')) {
    crds <- x@coords  
  } else crds <- x
  z <- deldir(crds[,1], crds[,2])
  w <- tile.list(z)
  polys <- vector(mode='list', length=length(w))
  require(sp)
  for (i in seq(along=polys)) {
    pcrds <- cbind(w[[i]]$x, w[[i]]$y)
    pcrds <- rbind(pcrds, pcrds[1,])
    polys[[i]] <- Polygons(list(Polygon(pcrds)), ID=as.character(i))
  }
  SP <- SpatialPolygons(polys)
  voronoi <- SpatialPolygonsDataFrame(SP, data=data.frame(x=crds[,1],
    y=crds[,2], row.names=sapply(slot(SP, 'polygons'), 
    function(x) slot(x, 'ID'))))
}

# rg0911 <- readOGR("shapefiles","RainGauges2009_2011")
# summary(rg0911)
# v <- voronoipolygons(rg0911)
# summary(v)
# plot(v)
# plot(rg0911, add = TRUE)
```

Proof that it works, but I think I need to do this to the subset for each season so that only points with observations are used. I should end up with 10 surfaces with varying numbers of polygons depending on how many sites were `NA` or removed b/c outliers for that season.

```{r create season Thiessen polys, eval=F}
library(rgdal); library(dismo)
rainy04v <- voronoi(rainy04 <- readOGR(dsn = "shapefiles", layer = "rain_gauges04"))
rainy05v <- voronoi(rainy05 <- readOGR(dsn = "shapefiles", layer = "rain_gauges05"))
rainy06v <- voronoi(rainy06 <- readOGR(dsn = "shapefiles", layer = "rain_gauges06"))
rainy07v <- voronoi(rainy07 <- readOGR(dsn = "shapefiles", layer = "rain_gauges07"))
rainy08v <- voronoi(rainy08 <- readOGR(dsn = "shapefiles", layer = "rain_gauges08"))

rainy09v <- voronoi(rainy09 <- readOGR(dsn = "shapefiles", layer = "rain_gauges09"))
rainy10v <- voronoi(rainy10 <- readOGR(dsn = "shapefiles", layer = "rain_gauges10"))
rainy11v <- voronoi(rainy11 <- readOGR(dsn = "shapefiles", layer = "rain_gauges11"))
rainy12v <- voronoi(rainy12 <- readOGR(dsn = "shapefiles", layer = "rain_gauges12"))
rainy14v <- voronoi(rainy14 <- readOGR(dsn = "shapefiles", layer = "rain_gauges14"))
```

Overlay Thiessen polygons and plot locations
```{r overlay plots and Thiessen polys, eval=F}
library(rgdal)
plts <- readOGR(dsn = "shapefiles", layer = "soco_plots_metric")
str(plts)
str(rainy04)
plts <- spTransform(plts, CRS=crs)
all.equal.default(proj4string(plts), proj4string(rainy04v), proj4string(rainy05v))

plot(elev, main = "2004 Rain Thiessens"); plot(plts, add = T, pch = 21); plot(rainy04, add = T, pch = 25, col = "blue"); plot(rainy04v, axes = T, add=T) 

summary(rainy05v)
plot(rainy05v, axes = T, main = "2005 Rain Thiessens"); plot(plts, add = T); plot(rainy05, add = T, col = "blue", pch = 25)

plot(rainy06v, axes = T, main = "2006 Rain Thiessens"); plot(plts, add = T); plot(rainy06, add = T, col = "blue", pch = 25)

plot(rainy07v, axes = T, main = "2007 Rain Thiessens"); plot(plts, add = T); plot(rainy07, add = T, col = "blue", pch = 25)

plot(rainy08v, axes = T, main = "2008 Rain Thiessens"); plot(plts, add = T); plot(rainy08, add = T, col = "blue", pch = 25)

plot(rainy09v, axes = T, main = "2009 Rain Thiessens"); plot(plts, add = T); plot(rainy09, add = T, col = "blue", pch = 25)

plot(rainy10v, axes = T, main = "2010 Rain Thiessens"); plot(plts, add = T); plot(rainy10, add = T, col = "blue", pch = 25)

plot(rainy11v, axes = T, main = "2011 Rain Thiessens"); plot(plts, add = T); plot(rainy11, add = T, col = "blue", pch = 25)

plot(rainy12v, axes = T, main = "2012 Rain Thiessens"); plot(plts, add = T); plot(rainy12, add = T, col = "blue", pch = 25)

plot(rainy14v, axes = T, main = "2014 Rain Thiessens"); plot(plts, add = T); plot(rainy14, add = T, col = "blue", pch = 25)
```

The voronoi (Thiessen) polygons don't extend to the edges of the extent as it is shrunk when the rain gauge station dataset is reduced around the edges. The bounding box appears to be based on the min-max of the `x` and `y` coordinates. It looks like none of them quite cover all of the plot point locations, but they would all be covered by the elevation surface.  

How do I extend the `bbox` of the point shapefiles to the elevation surface extent and have that captured by the `voronoi` function?

```{r extend bbox, eval=F}
bbox(elev)
bbox(rainy04)
all.equal(proj4string(elev), proj4string(rainy04))
elev@extent
new_bbox <- bbox(elev)
rainy04@bbox <- new_bbox
bbox(rainy04)
test <- voronoi(rainy04)
plot(test); plot(plts, add = T)
extent(rainy04)
```

Since the `voronoi` function pulls the point coordinates explicitly it is apparent that this won't work the way I want it to.


Developing a linear model for kriging
```{r rk model jan 2008}
rg_0108 <- subset(rg_2003_2008@data, year==2008 & month==1)
coordinates(rg_0108) = ~NORTHING+EASTING
str(rg_0108)
#rg_2003_2008 <- readOGR(dsn="shapefiles", layer="RainGauges2003_2008")
#rg_2003_2008 <- spTransform(rg_2003_2008, CRS=crs)
rg_0108@proj4string <- rg_2003_2008@proj4string
rg_0108 <- spTransform(rg_0108, CRS=crs)

# elev_ppt <- overlay(as(elev, "SpatialGridDataFrame"), rg_0108)   # create grid-points overlay
   # copy the slope values
lm_rg_0108 <- lm(daily_ppt ~ elev_m, rg_0108)# This is certainly incorrect b/c precipitation constrained to be >= 0 
summary(lm_rg_0108)
plot(lm_rg_0108)

plot(log(rg_0108$elev_m), log1p(rg_0108$daily_ppt))
lm_rg_0108a <- lm((daily_ppt) ~ elev_m, data = rg_0108@data %>% filter(NAME != "backman", day == 31))
summary(lm_rg_0108a)
```


```{r thin plate spline example}
library(fields)
loc_matrix <- cbind(rg_0108$NORTHING,rg_0108$EASTING)
ppt <- (rg_0108$daily_ppt)
jan08tps <- Tps(loc_matrix, ppt)
summary(jan08tps)
plot(jan08tps)
surface(jan08tps)

look <- predict(jan08tps, lambda = 2.0)
look <- predict(jan08tps, df = 4)

tps1 <- Tps(loc_matrix, ppt, df = 4)
summary(tps1)
set.panel(2,2)
plot(tps1)
set.panel()

out_jan08 <- predictSurface(jan08tps)
image(out_jan08)

# fit <- Tps(ozone$x, ozone$y)
# set.panel(2,2)
# plot(fit)
```
